environment:
  name: "staging"
  type: "local"

deployers:
- name: "default"
  rpc: http://localhost:8547
  verifyContracts: false
  verifierUrl: http://localhost:4000/api
  slow: false

eigenda:
  deployer: "default"

privateKeys:

services:
  counts:
    dispersers: 1
    operators: 4
  stakes:
    total: 100e18
    distribution: [1.3, 2, 3, 5]
  basePort: 32000
  variables:
    globals:
      HOSTNAME: localhost
      TIMEOUT: 20s
      CHAIN_RPC: http://localhost:8547
      CHAIN_ID: 40525
      G1_PATH: ../test/resources/kzg/g1.point.300000
      G2_PATH: ../test/resources/kzg/g2.point.300000
      CACHE_PATH: ../test/resources/kzg/SRSTables
      SRS_ORDER: 300000
      CHALLENGE_ORDER: 300000
      STD_LOG_LEVEL: "trace"
      VERBOSE: true
